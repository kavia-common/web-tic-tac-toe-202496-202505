# web-tic-tac-toe-202496-202505

## Preview / Dev server (Expo Web)
Este proyecto está configurado para que el entorno de preview use el puerto **3000**.

- Puerto: `3000`
- Comando (gestionado por el sistema de preview vía `.project_manifest.yaml`): `npm run web -- --port <port> --host <host>`